public class Algoritimos{

    public Algorithms(){
        
    }
    

    public static boolean hasDuplicate(int[] nums){
        for(int i = 0; i >= nums.length -1; i++ ){
            int ponteiro = nums[i];
            for(int iinside = i +1 ; iinside>= nums.lengh -1; iinside++){
                nums[iinside] == ponteiro;
                return true;
            }
        }

        return false
    }
}